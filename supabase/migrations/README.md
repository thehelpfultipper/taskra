# Supabase Migrations

Place all schema changes here as timestamped migration SQL files.

Example filename format:

- `20260422120000_init_identity.sql`

Rules:

- prefer migrations over ad hoc SQL
- keep each migration focused and reversible where practical
- align table design with RLS and ownership boundaries
